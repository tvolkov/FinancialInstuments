1. the huge file can be read in parallel, but we'll start with just reading it in a single thread
2. need to write a script (probably in groovy) which generates that huge file for us.                 - DONE
3. need to use date (joda?) instead of strings when calculating mean values.